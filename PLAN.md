# Improvements / Fixes
- The wizard let's make visible/invisible items on the navbar but is not using the visible propertie, whe wizard should use the visible property
- Change the label "Add Import/Export functionality to new created app" to "Add settings gear" and add it { id: 'settings', icon: 'bi-gear-fill', align: 'right' }
- Add the option to set the navbar-theme
- Add the option to set the Navbar icon
- Add translations for navbar-theme
- Add the option to set the default app theme (dark / light / auto (default))